'use client';
import {useEffect,useRef,useState} from 'react';
import {BookOpen,HeartHandshake} from 'lucide-react';
import {usePublic} from './provider';
const demoPortraits=[1,2,3].map(n=>({id:'demo-'+n,name:'Illustrative portrait '+n,image:'/portraits/demo-numerologist-'+n+'.png'}));
export function Experts(){const {data}=usePublic();const experts=data?.experts.length?data.experts:demoPortraits;const [index,setIndex]=useState(0);const [hover,setHover]=useState(false);const [reduced,setReduced]=useState(false);const touch=useRef(0);useEffect(()=>{const mq=matchMedia('(prefers-reduced-motion: reduce)');const update=()=>setReduced(mq.matches);update();mq.addEventListener('change',update);return()=>mq.removeEventListener('change',update);},[]);useEffect(()=>{if(hover||reduced||experts.length<2)return;const id=setInterval(()=>{if(!document.hidden)setIndex(i=>(i+1)%experts.length);},4500);return()=>clearInterval(id);},[hover,reduced,experts.length]);function move(d:number){setIndex(i=>(i+d+experts.length)%experts.length);}return <section className="expert-section" id="numerologists"><div className="container expert-grid"><div><div className="eyebrow">THE PEOPLE BEHIND YOUR PERSPECTIVE</div><h2>Ancient wisdom.<br/><em>A human connection.</em></h2><p>A personal report begins with someone taking the time to understand your questions. Our readings bring care and perspective to your story.</p><div className="feature-line"><BookOpen size={19}/> Personal interpretation, clearly explained</div><div className="feature-line"><HeartHandshake size={19}/> Prepared and reviewed by a real person</div></div><div className="expert-carousel" aria-label="Our numerologists" onMouseEnter={()=>setHover(true)} onMouseLeave={()=>setHover(false)} onFocus={()=>setHover(true)} onBlur={e=>{if(!e.currentTarget.contains(e.relatedTarget))setHover(false);}} onTouchStart={e=>touch.current=e.touches[0].clientX} onTouchEnd={e=>{if(experts.length>1&&Math.abs(e.changedTouches[0].clientX-touch.current)>50)move(e.changedTouches[0].clientX<touch.current?1:-1);}}><div className="portrait-stage">{experts.map((expert,i)=>{const relative=(i-index+experts.length)%experts.length;const position=relative===0?'center':relative===1?'right':relative===experts.length-1?'left':'hidden';return <div key={expert.id} className={'portrait '+position} aria-hidden={relative!==0}><img src={expert.image} alt={expert.name} width={500} height={650}/></div>;})}</div></div></div></section>;}
